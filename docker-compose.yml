services:
  frontend:
    image: ${FRONTEND_IMAGE:-ghcr.io/thenrique395/investindoemnegociosweb:latest}
    ports:
      - "${FRONTEND_PORT:-4200}:4000"
    environment:
      PORT: 4000
      API_BASE_URL: ${API_BASE_URL}
    restart: unless-stopped
